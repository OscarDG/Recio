---
// InfiniteTextLoop.astro
// Infinite horizontal scrolling text loop - Midlife Engineering style
---

<section class="relative overflow-hidden bg-white mt-20 py-20">
    <!-- Container with mask/fade edges -->
    <div class="relative">     
      <!-- Scrolling container -->
      <div class="infinite-scroll-wrapper flex">
        <!-- First set of items -->
        <div class="infinite-scroll-content flex items-center whitespace-nowrap">
          <h1 class="inline-flex items-center text-main-black text-2xl md:text-6xl font-bold mx-5">EL CARACTER</h1>
          <span class="inline-flex items-center"><img src="./images/recio_redes.png" alt="Recio Logo" class="w-10 h-10"/></span>
          <h1 class="inline-flex items-center text-main-black text-2xl md:text-6xl font-bold mx-8">NO SE IMPROVISA
          </h1>
          <span class="inline-flex items-center"><img src="./images/recio_redes.png" alt="Recio Logo" class="w-10 h-10"/></span>
        </div>
        <div class="infinite-scroll-content flex items-center whitespace-nowrap">
            <h1 class="inline-flex items-center text-main-black text-2xl md:text-6xl font-bold mx-8">EL CARACTER</h1>
            <span class="inline-flex items-center"><img src="./images/recio_redes.png" alt="Recio Logo" class="w-10 h-10"/></span>
            <h1 class="inline-flex items-center text-main-black text-2xl md:text-6xl font-bold mx-8">NO SE IMPROVISA
            </h1>
            <span class="inline-flex items-center"><img src="./images/recio_redes.png" alt="Recio Logo" class="w-10 h-10"/></span>
        </div>
      </div>
    </div>
  </section>
  <style>
    /* Horizontal infinite scroll animation */
    .infinite-scroll-wrapper {
      animation: scroll-horizontal 30s linear infinite;
    }
  
    .infinite-scroll-content {
      flex-shrink: 0;
    }

  
    /* Horizontal scroll keyframes */
    @keyframes scroll-horizontal {
      0% {
        transform: translateX(0);
      }
      100% {
        transform: translateX(-50%);
      }
    }
  
    /* Pause animation on hover (optional) */
    .infinite-scroll-wrapper:hover {
      animation-play-state: paused;
    }
  
    /* Prevent text selection for better UX */
    .infinite-scroll-wrapper{
      user-select: none;
    }
  </style>