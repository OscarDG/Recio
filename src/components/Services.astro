---
import { services } from "../lib/services"
import type { Service } from "../types/services"

import '../styles/global.css'
import Banner from './Banner.astro'
import CtaBtn from './CtaBtn.astro'

const LinkText = encodeURI('Hola, quisiera conocer más de sus servicios')
const WhatsAppNumber = `https://wa.me/573174048898?text=${LinkText}`
---

<section class="scroll-reveal-section min-h-screen bg-linear-to-b from-slate-50 to-white py-20 font-[montserrat]">
  <div class="container relative flex flex-col justify-center items-center mx-auto px-6">
 <!-- Header -->
    <div class="reveal-item flex flex-col w-full opacity-0 translate-y-12 transition-all duration-1000 mb-16">
      <h2 class="text-5xl md:text-6xl font-bold text-main-black mb-4">
        SERVICIOS
      </h2>
      <p class="text-xl text-slate-600 max-w-2xl">
        Trabajamos junto a ti para hecer que tu marca deje huella
      </p>
    </div>
    <!-- Grid of Cards -->
    <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8 mb-20">
      {
        services.map((service) => (
          <div class="reveal-item opacity-0 translate-y-12 transition-all duration-1000" style="transition-delay: 100ms;">
                  <div class="bg-white rounded-2xl p-8 shadow-lg hover:shadow-2xl transition-shadow duration-300">
                    <div class="w-16 h-16 bg-linear-to-br from-purple-500 to-pink-500 rounded-xl mb-6 flex items-center justify-center">
                      <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z"></path>
                      </svg>
                    </div>
                    <h3 class="text-2xl font-bold text-main-black mb-3">{service.name}</h3>
                    <p class="text-slate-600 leading-relaxed mb-5">{service.description}</p>
                    <ul class="flex flex-col justify-around gap-2">
                      {
                        service.works.map((item) => (
                          <li class="flex flex-row items-center"><span class="block w-3 h-3 rounded-full mr-2 bg-main-orange after:content['']"></span>{item.description}</li>
                        ))
                      }
                    </ul>
                  </div>
          </div>
        ))
      }
    </div>
    <!-- Call to Action -->
    <div class="reveal-item flex flex-col justify-center items-center w-full md:w-[50%] opacity-0 translate-y-12 transition-all duration-1000 text-center">
      <h3 class="text-3xl font-bold text-main-black mb-10">¿Listo para iniciar tu proyecto?</h3>
      <div class="w-50 h-15">
        <CtaBtn text="Contactarnos" link={WhatsAppNumber} variant="primary"/>
      </div>
    </div>
  </div>
    <Banner />
</section>
<script>
  // Intersection Observer for scroll-triggered animations
  const observerOptions = {
    threshold: 0.1,
    rootMargin: '0px 0px -100px 0px'
  };

  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add('revealed');
        entry.target.classList.remove('opacity-0', 'translate-y-12');
        entry.target.classList.add('opacity-100', 'translate-y-0');
      }
    });
  }, observerOptions);

  // Observe all elements with the reveal-item class
  document.addEventListener('DOMContentLoaded', () => {
    const revealItems = document.querySelectorAll('.reveal-item');
    revealItems.forEach(item => observer.observe(item));
  });

  // Re-observe on Astro view transitions (if using)
  document.addEventListener('astro:page-load', () => {
    const revealItems = document.querySelectorAll('.reveal-item');
    revealItems.forEach(item => observer.observe(item));
  });
</script>

<style>
  .revealed {
    /* Class added when element comes into view */
  }
</style>